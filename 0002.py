N=int(input())
lst=list(range(N+1))
value=0
for i in lst:
    value+=i
print(value)
#append()..is used to add an item to the end of the list
#insert()..is used to insert a new item at the given position in the list
#index()..finds the first occurrence of a list item and returns its index
#max(list)...returns the maximum value
#min(list)...returns the minimum value
#list.count(item)..returns a count of how many times an item occurs in list
#list.remove(item)..removes an item from a list
#list.reverse()...reverses items in a list
#format()....this function enables values to be embedded in it using placeholders
#Each arguement of the format is placed in the corresponding position which is dertemined using curly braces
#join()...join a list strings with another string a separator
#split()...is the opposite of join()and it turns a string with a certain separator into a list
#replace()
#upper()
#lower()